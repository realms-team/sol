language: python
python:
  - "2.7"

before_install:

install:
  - git clone --branch=master https://github.com/twatteyne/smartmeshsdk.git /home/travis/build/realms-team/smartmeshsdk
  - pip install -r requirements.txt

script:
  - python -m pytest tests/
